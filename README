# HPC Demos

## Purpose
This repo provides basic examples for MPI and OpenMP programs. Each directory has one file written with mpi (e.g. hello_world_mpi.c), one with openmp (e.g. hello_world_omp.c), and one serial version (e.g. hello_world_serial.c), which means no parallelization.